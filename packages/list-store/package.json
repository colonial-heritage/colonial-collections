{
  "name": "@colonial-collections/list-store",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "private": true,
  "devDependencies": {
    "eslint-config-custom": "*",
    "jest": "29.5.0",
    "ts-jest": "29.1.0",
    "tsconfig": "*"
  },
  "scripts": {
    "test": "jest",
    "test:ci": "npm test -- --ci",
    "test:watch": "npm test -- --watchAll"
  },
  "dependencies": {
    "next-intl": "2.13.0-beta.2",
    "react": "18.2.0",
    "zod": "3.21.4",
    "zustand": "4.3.7"
  },
  "engines": {
    "node": "18.x"
  },
  "jest": {
    "preset": "ts-jest",
    "testTimeout": 60000,
    "testMatch": [
      "<rootDir>/src/**/*.test.ts(x)?"
    ],
    "collectCoverage": true,
    "collectCoverageFrom": [
      "<rootDir>/src/**/*.ts(x)?"
    ]
  }
}
